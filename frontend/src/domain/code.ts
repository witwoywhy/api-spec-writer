export type ErrorCode = {
  id: string;
  status: string;
  code: string;
  message: string;
  description: string;
};

export type EventCode = {
  id: string;
  code: string;
  name: string;
  description: string;
};
